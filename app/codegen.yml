overwrite: true
schema: http://localhost:8080/
documents: ./graphql/*.graphql
generates:
  ./generated/graphql.d.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
  ./graphql.schema.json:
    plugins:
      - "introspection"
